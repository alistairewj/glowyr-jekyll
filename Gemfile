source "https://rubygems.org"

# Jekyll site for Glowyr Inc. using TeXt theme

gem "jekyll", "~> 4.3"
# TeXt theme as a gem (works with GitHub Pages when building via Actions)

gem "jekyll-text-theme"

group :development do
  # Needed for local `jekyll serve` on Ruby 3+
  gem "webrick", "~> 1.8"
end
